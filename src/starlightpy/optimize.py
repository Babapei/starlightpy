"""Placeholder. PLAN phase C is annealing of (A_V, v, σ) only — never of x_j."""

from __future__ import annotations


def metropolis_anneal(*_args, **_kwargs):
    raise NotImplementedError(
        "Annealing of (A_V, v, sigma) is PLAN phase C, and x_j stays NNLS. "
        "Finish phase B first. See docs/PLAN.md."
    )
