# 进度 / 断点

中断后只看本文「当前断点」和下一小段。目标仍以 [PLAN.md](PLAN.md) 为准。

## 当前断点

| 项 | 值 |
| --- | --- |
| 阶段 | D（clip 与稀疏 \(x\)） |
| 小段 | **B3 已完成**（2026-09-01）；C 已跳过 |
| 下一小段 | D：NSIGMA clip 再拟合 + `x_min_keep`（EX0） |
| 不要做 | 退火、真星系、对 \(x_j\) 做 Metropolis；不要为跳过的 C 填 `optimize.py` |

## 小段清单

### 阶段 A — 框架

- [x] 双包、前向模型、固定 \(v,\sigma=0\) 时收回 \(x,A_V\)
- [x] LOSVD 在均匀 lnλ；\(v>0\) 吸收往红端

### 阶段 B

- [x] **B1** `starlightpy.simulate`：吸收线模板 + `mock_observation`（与 `fit_spectrum` 同一套红化/混合/LOSVD）。无噪声、**告诉拟合器真实 \(v,\sigma\)** 时仍收回 \(x,A_V\)。
- [x] **B2** 打开运动学网格搜索（先改 PLAN 白名单：`v_bounds` 等）
- [x] **B3** 真值不落网格点时收回；无噪声容差不宽于 B2；第二组 \(x,A_V\)

### 阶段 C–F

- [x] **C 跳过**：B3 网格已够，不对 \(x_j\) 退火。
- [ ] D clip + EX0
- [ ] E `.cxt` / gzip / 批量 / 可选 SDSS FITS

## B2 验收（先写再做，2026-09-01）

实现：`search_kinematics=True` 时外层网格扫 \(v,\sigma\)，每个点内层仍 \(A_V\)+NNLS；`False` 时行为与 B1 相同。

合成：`default_absorption_bases`，真值 \(x=(0.50,0.35,0.15)\)，\(A_V=0.30\)，\(v=100\)，\(\sigma=150\)（真值落在测试网格上）。走 `fit_spectrum`，不告诉真值 \(v,\sigma\)。

| 路径 | 网格（测试用，比默认密/小） | 断言 |
| --- | --- | --- |
| 无噪声 | \(v\in\{0,50,100,150,200\}\)，\(\sigma\in\{50,100,150,200\}\)，\(A_V\) 步长 0.1、范围 [0,0.6] | \(|\Delta v|\le 50\)，\(|\Delta\sigma|\le 50\)，\(|\Delta A_V|\le 0.10\)，\(x\) atol 0.12 |
| SNR=30 | 同上 | \(|\Delta v|\le 100\)，\(|\Delta\sigma|\le 100\)，\(|\Delta A_V|\le 0.20\)，\(x\) atol 0.20 |
| 门禁 | 1×1 运动学点，确认不再 `NotImplementedError` | 退火/clip 仍未实现 |

失败则记本段「未通过」原因，不开始 B3。

**B2 结果（2026-09-01）：通过。** 无噪声与 SNR=30 均达上表断言；门禁 1×1 网格不再抛未实现。未开始 B3。

默认配置网格可更宽，测试用上面缩小的范围以免一次 NNLS 过多。

## B3 验收（先写再做，2026-09-01）

B2 真值落在网格上，无法区分「χ² 最小」和「碰巧踩点」。B3 把真值放在节点之间；网格步长 25 km/s，容差不宽于 B2。

走 `fit_spectrum`（`search_kinematics=True`），断言在 `tests/starlightpy/test_kinematics_search.py`。无噪声。

| 路径 | 真值 | 测试网格 | 断言 |
| --- | --- | --- | --- |
| 离网 1 | \(x=(0.50,0.35,0.15)\)，\(A_V=0.30\)，\(v=80\)，\(\sigma=130\) | \(v\in[50,125]\) 步长 25；\(\sigma\in[100,175]\) 步长 25；\(A_V\in[0,0.6]\) 步长 0.1 | \(|\Delta v|\le 25\)，\(|\Delta\sigma|\le 25\)，\(|\Delta A_V|\le 0.10\)，\(x\) atol 0.12 |
| 离网 2 | \(x=(0.20,0.55,0.25)\)，\(A_V=0.35\)，\(v=120\)，\(\sigma=90\) | \(v\in[50,175]\) 步长 25；\(\sigma\in[50,150]\) 步长 25；\(A_V\in[0,0.6]\) 步长 0.1 | 同上 |

失败则记本段「未通过」原因，不开始 D。

**B3 结果（2026-09-01）：通过。** 两组离网无噪声均 \(|\Delta v|\le 25\)、\(|\Delta\sigma|\le 25\)。C 跳过。

## D 验收（先写再做，2026-09-01）

`clip_nsigma` 默认 `None`（不 clip）。设置后：第一次拟合 → \(|O-M|>n\sigma e_\lambda\) 的像素权重置 0 → **冻结** \((v,\sigma)\) 再做一次 \(A_V\)+NNLS。`x_min_keep>0` 时丢掉过小光度分数再拟合（EX0）。运动学不重搜。走 `fit_spectrum`。

| 路径 | 做法 | 断言 |
| --- | --- | --- |
| clip | 无噪声合成谱打一个尖峰（不在归一窗口） | `n_clipped>=1`；\(|\Delta A_V|\le 0.10\)，\(x\) atol 0.12 |
| EX0 | 真值 \(x=(0.55,0.40,0.05)\)，`x_min_keep=0.10` | 第三分量 \(x=0\)；其余分数接近丢掉后的归一 |
| 门禁 | `metropolis_anneal` 仍 `NotImplementedError` | clip 不再抛未实现 |

## 日志

| 日期 | 小段 | 做了什么 |
| --- | --- | --- |
| 2026-08-31 | A | 框架、合同、LOSVD 修正 |
| 2026-09-01 | B1 | `simulate.py` + 固定真值 \(v,\sigma\) 收回 \(x,A_V\) |
| 2026-09-01 | B2 | 外层 \(v,\sigma\) 网格；无噪声 + SNR=30 测试 |
| 2026-09-01 | B3 | 离网 \(v,\sigma\) 收回；C 跳过 |
